#!/usr/bin/env bash
set -euo pipefail

echo "--- rustfmt ---"
cargo fmt --all -- --check

echo "--- clippy ---"
cargo clippy --workspace --all-targets --all-features -- -D warnings

echo "Pre-commit checks passed."
